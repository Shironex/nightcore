// Sidebar is presentational — projects, nav, and collapse state are owned by the
// shell and passed in as props. This module exists to satisfy the
// folder-per-component sibling contract.
export {};
