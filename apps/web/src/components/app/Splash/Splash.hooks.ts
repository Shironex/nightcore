// Splash is a pure presentational boot screen with no local state — the parent
// shell owns the splash-visible timer. This module exists to satisfy the
// folder-per-component sibling contract.
export {};
