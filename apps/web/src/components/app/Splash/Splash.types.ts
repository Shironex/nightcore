export interface SplashProps {
  /** A short boot line under the loader (e.g. "loading projects…"). */
  bootLine?: string;
  /** App version shown at the foot of the splash. */
  version?: string;
}
