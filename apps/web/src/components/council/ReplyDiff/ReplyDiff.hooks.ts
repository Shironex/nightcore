// ReplyDiff is a pure presentational binding — it renders the broadcast rounds the
// parent grouped from the transcript (via `../reply-diff`), with no local state,
// effects, or queries. This module exists to satisfy the folder-per-component sibling
// contract and is the home for any future state the diff grows (e.g. collapse a round).
export {};
