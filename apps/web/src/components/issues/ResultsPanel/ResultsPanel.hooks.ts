// ResultsPanel is a pure presentational card: the verdict stream and the action
// handlers are owned upstream by the IssueTriageView hook. This module exists to
// satisfy the folder-per-component sibling contract and is the home for any future
// state.
export {};
