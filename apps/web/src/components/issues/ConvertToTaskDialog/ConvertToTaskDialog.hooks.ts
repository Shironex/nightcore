// ConvertToTaskDialog is a pure presentational confirmation: the open flag, the
// converting/error state, and every derived preview label are owned upstream by the
// IssueTriageView hook. This module exists to satisfy the folder-per-component
// sibling contract and is the home for any future state.
export {};
