// AutoModeOptions is settings content only — popover open/close lives in
// `ToolbarOption.hooks.ts`. This module satisfies the folder-per-component
// sibling contract and is the home for any future state this component grows.
export {};
