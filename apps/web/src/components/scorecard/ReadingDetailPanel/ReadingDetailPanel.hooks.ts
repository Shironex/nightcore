// ReadingDetailPanel is a pure presentational sheet with no local state, effects,
// or queries — its harden action is a handler passed in from ScorecardView, and the
// `pending` flag is owned upstream. This module exists to satisfy the
// folder-per-component sibling contract and is the home for any future state.
export {};
