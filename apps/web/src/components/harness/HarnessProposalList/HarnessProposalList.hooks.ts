// HarnessProposalList is a pure presentational panel with no local state,
// effects, or queries — the artifacts, loading flag, and empty message are
// derived upstream in the HarnessView hook (grouping is a module-level pure
// transform). This module exists to satisfy the folder-per-component sibling
// contract and is the home for any future state this component grows.
export {};
