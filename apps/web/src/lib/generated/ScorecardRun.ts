// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { InsightUsage } from "./InsightUsage";
import type { StoredReading } from "./StoredReading";

/**
 * One Scorecard run, persisted under `.nightcore/scorecards/<id>.json`. Reuses the
 * Insight [`InsightUsage`] token totals so the two features share one usage shape.
 */
export type ScorecardRun = { id: string, projectPath: string, 
/**
 * `running` | `completed` | `failed`.
 */
status: string, 
/**
 * The dimensions requested for this run (wire strings).
 */
dimensions: Array<string>, model: string, createdAt: number, updatedAt: number, costUsd: number, durationMs: number, usage: InsightUsage, readings: Array<StoredReading>, error: string | null, };
