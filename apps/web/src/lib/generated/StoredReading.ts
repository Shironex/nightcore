// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { FindingLocation } from "./FindingLocation";
import type { ScorecardEvidence } from "./ScorecardEvidence";

/**
 * A persisted reading: the engine's grading output plus the Rust-owned lifecycle
 * fields (`status`, `linkedTaskId`). `dimension`/`grade`/`status` are stored as
 * their wire strings (the web casts them to its unions) so this struct never has
 * to mirror the contract enums.
 */
export type StoredReading = { id: string, dimension: string, 
/**
 * `A` | `B` | `C` | `D` | `E` | `F`.
 */
grade: string, title: string, summary: string, rationale: string | null, location: FindingLocation | null, suggestion: string | null, affectedFiles: Array<string>, tags: Array<string>, findings: Array<ScorecardEvidence>, confidence: number | null, fingerprint: string, 
/**
 * Lifecycle: `open` | `converted`.
 */
status: string, 
/**
 * The board task this reading was hardened into, if any.
 */
linkedTaskId: string | null, };
